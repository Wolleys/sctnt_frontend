import { FC } from "react";
import "./header.css";

const Header: FC = () => {
    return (
        <header className="header">
            <h1>Supply Chain Track and Trace System</h1>
        </header>
    );
};

export default Header;
